{
  "compilerOptions": {
    "target": "es2016",
    "module": "none", // or "system", depending on your preference
    "outFile": "./LinnstrumentLooping.control.js",
    "rootDir": ".", // assuming your TypeScript files are in a directory named src
    "types": ["typed-bitwig-api"]
    // other options as needed
  },
  "files": [
    "./src/Modules/Base/ControllerModule.ts",
    "./src/Modules/Base/ClipControllerModule.ts"
  ],
  "include": [
    "./src/**/*.ts" // replace with the actual path to your TypeScript files
  ],
  "exclude": [
    "node_modules" // typical exclude
  ]
}
